#ifndef NETWORK_H\n#define NETWORK_H\n\nclass Client {\npublic:\n    void connect(const char* address, int port);\n    void disconnect();\n    void sendData(const void* data, size_t size);\n    void receiveData(void* buffer, size_t size);\n};\n\nclass Server {\npublic:\n    void start(int port);\n    void stop();\n    void acceptClient();\n    void sendDataToClient(Client& client, const void* data, size_t size);\n};\n\n#endif // NETWORK_H